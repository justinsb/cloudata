## Customize checkout
checkout:
  post:
    - git submodule sync
    - git submodule update --init # use submodules

## Customize dependencies commands
#dependencies:
#  pre:
#    - cd barge; mvn install -Dmaven.test.skip=true
#    - cd cloudata-shared; mvn install
